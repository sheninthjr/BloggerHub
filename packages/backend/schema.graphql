type User {
    id: ID!
    email: String!
    firstname: String!
    lastname: String!
}

input CreateUserInput {
    email: String!
    firstname: String!
    lastname: String!
}

type Mutation {
    CreateUser(input:CreateUserInput!): User!
}