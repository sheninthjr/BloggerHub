type User {
    id: ID
    email: String!
    firstname: String!
    lastname: String!
}

type Query {
    user: [User]
}

input CreateUserInput {
    email: String!
    firstname: String!
    lastname: String!
}

type Mutation {
    CreateUser(input:CreateUserInput!): User!
}